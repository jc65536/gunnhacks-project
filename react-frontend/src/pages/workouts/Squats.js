import { login, authFetch, useAuth, logout } from "../../auth"
import React, { useEffect, useState } from "react"

class Squats extends React.Component {
    render() {
        return (
            <div>
                <h2>Welcome to the squats workout</h2>
            </div>
        )
    }
}

export default Squats;